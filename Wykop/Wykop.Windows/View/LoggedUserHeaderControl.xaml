<UserControl
    x:Class="Wykop.View.LoggedUserHeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Wykop.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" Background="Transparent">

    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Stretch"
          Background="Transparent" Orientation="Horizontal">
        <StackPanel.Resources>
            <Style x:Name="DefaultTagStyle" TargetType="TextBlock" BasedOn="{StaticResource TitleTextBlockStyle}">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="5 0 5 0"></Setter>
            </Style>
        </StackPanel.Resources>

        <TextBlock Style="{StaticResource DefaultTagStyle}">
            <Run Text="#pm"/>
            <Run Text="{Binding PrivateMessageNotificationCount}"></Run>
        </TextBlock>

        <TextBlock Style="{StaticResource DefaultTagStyle}">
            <Run Text="#"/>
            <Run Text="{Binding HashTagsNotificationCount}"></Run>
        </TextBlock>

        <Image DataContext="{Binding LoggedUserProfile}" Source="{Binding DefaultAvatar}" Stretch="None" Margin="15 0 0 0"/>
    </StackPanel>
</UserControl>
